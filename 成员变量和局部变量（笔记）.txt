属性对应是类中变量，行为对应的类中的函数（方法）。
其实定义类，就是在描述是事物，就是在定义属性和行为。属性和行为共同成为类中的成员（成员变量和成员方法）。


作用范围：
成员变量作用于整个类中。
局部变量作用于函数中，或者语句中。
在内存中的位置：
成员变量：在堆内存中，因为对象的存在， 才在内存中存在。
局部变量：存在栈内存中。




































